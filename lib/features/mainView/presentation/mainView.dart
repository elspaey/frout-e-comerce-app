import 'package:flutter/material.dart';

class Mainview extends StatelessWidget {
  const Mainview({super.key});
  static const routName = "mainView";
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
